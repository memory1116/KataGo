# KataGo SM86 版使用说明

本版本把 tactic plan 当作可选的性能预设，而不是运行许可证。

## 可以自由更换的内容

- 任意 KataGo 模型，不校验模型 SHA-256。
- 任意 SM86 显卡，不校验 RTX 3080 Ti 产品名、显存、SM 数或 L2 大小。
- `nnMaxBatchSize`、`numNNServerThreadsPerModel`（lane 数）、搜索线程数。
- CUDA、cuDNN、Python 的安装位置和版本，只要能够正常构建 KataGo。
- FP16、NHWC、CUDA Graph 和调度器设置；配置文件中的用户设置优先于 plan。

唯一保留的硬件安全检查是 CUDA 架构：SM86 plan 只能在 compute capability 8.6 上加载。
不符合专用内核模型形状、非 19x19 或实际 batch 与 tactic batch 不同时，会自动使用官方 CUDA
forward，不会拒绝模型或强迫用户修改配置。

## 编译

已经有 Python 3、CUDA、cuDNN、CMake、Ninja 和所需 Flash-Attention/TileLang 源码时，
不需要先运行 `setup.sh`：

```bash
cd /root/KataGo-fixed
./build-for-plan.sh --plan final-migration/plans/sm86/rtx3080ti-b8-s4/best-tactic-plan.json
```

可用环境变量指定自己的环境：

```bash
KATAGO_PYTHON=/usr/bin/python3 \
CUDACXX=/usr/local/cuda/bin/nvcc \
CUDNN_ROOT=/usr/local/cudnn \
SM89_FLASH_ATTN_ROOT=/path/to/patched/flash-attention \
KATAGO_TILELANG_ROOT=/path/to/TileLang \
./build-for-plan.sh --plan /path/to/best-tactic-plan.json
```

`setup.sh` 仍可使用，但只是一键下载和准备依赖的便利工具。

普通官方式 CUDA 编译也仍然可用：

```bash
cmake -S cpp -B cpp/build -G Ninja \
  -DUSE_BACKEND=CUDA -DCMAKE_BUILD_TYPE=Release \
  -DKATAGO_CUDA_ARCHITECTURES=86
cmake --build cpp/build --parallel
```

## 运行

选择任意模型、batch 和 lane：

```bash
./run.sh \
  --katago /path/to/katago \
  --model /path/to/model.bin.gz \
  --plan final-migration/plans/sm86/rtx3080ti-b8-s4/best-tactic-plan.json \
  --batch 16 \
  --lanes 2
```

`--batch` 和 `--lanes` 不传时，完全采用配置文件里的值。plan 只有一个 tactic mapping 时会自动
选择；多个 mapping 时用 `--tactic-batch N`。不想加载 plan 时：

```bash
./run.sh --no-plan --katago /path/to/katago --model /path/to/model.bin.gz
```

也可以不用 `run.sh`，直接按官方方式启动并在配置中添加 plan：

```bash
/path/to/katago gtp -model /path/to/model.bin.gz -config /path/to/gtp.cfg \
  -override-config cudaTacticPlanFile=/path/to/best-tactic-plan.json
```

## 本机验证结果

验证设备为 RTX 3080 Ti（SM86），模型为 `/root/work/data/weights/28b.bin.gz`，条件为
B8、4 lanes、48 搜索线程。

- 完整 CUDA 13/cuDNN 9/SM86 编译通过。
- 65 项 Python 工作流单测通过。
- 短 ABBA：新引擎平均 1508.87 nnEvals/s，旧引擎 1500.21，约 +0.58%。
- 长 ABBA（v3200/n10）：新引擎平均 1496.99 nnEvals/s，旧引擎 1498.05，约 -0.07%，
  属正常波动，性能持平。
- 另用不同小模型、B4、2 lanes 验证，成功自动回退官方 CUDA 并完成推理。

最终验证二进制：`/root/KataGo-fixed/katago-sm86`（另复制到 `/root/katago-sm86`）。
测速原始日志：`/root/KataGo-fixed/perf-abba` 和 `/root/KataGo-fixed/perf-abba-long`。
